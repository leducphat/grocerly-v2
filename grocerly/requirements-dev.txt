# Development and test dependencies. Not installed in the production image.
# Usage (from grocerly/): pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-django==4.14.0
pytest-cov==7.1.0
ruff==0.16.8
jscpd==5.2.1

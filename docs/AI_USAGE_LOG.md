# AI USAGE LOG — Grocerly (KLTN)

Nhật ký sử dụng LLM / Agentic AI trong quá trình làm đồ án.

| | |
|---|---|
| Sinh viên | Lê Đức Phát — 21139083 |
| Bắt đầu ghi | 10/09/2026 |
| Phục vụ tiêu chí | TC2.3 (5 điểm) — *Mức độ làm chủ và năng lực kiểm soát khi sử dụng LLM / Agentic AI* |

---

## Vì sao có file này

Rubric KLTN nói rõ: **dùng AI không bị trừ điểm**. Thứ bị chấm là mức độ sinh viên
làm chủ sản phẩm. Cụ thể:

- Không nộp nhật ký này (và cũng không nộp bản cam kết không dùng AI) → gate **G3**
  → TC2.3 bị chặn ở **2,75/5**.
- Nội dung kê khai mâu thuẫn với lịch sử Git hoặc với phần trả lời vấn đáp → gate
  **G4** → TC2.3 = **0 điểm**, đồng thời kích hoạt quy trình xác định đạo văn.
- Ngưỡng Mức 5: nhật ký **đối chiếu được với lịch sử commit**, giải thích đúng
  **100%** vị trí mã do AI sinh khi hội đồng chọn ngẫu nhiên ≥ 5 chỗ, và nêu được
  **≥ 5 lỗi/ảo giác của AI đã tự phát hiện** kèm phân tích nguyên nhân và commit sửa.

Nói cách khác: file này không phải thủ tục hành chính. Nó là bằng chứng cho câu
"em hiểu code của em", và nó chỉ có giá trị nếu được ghi *ngay khi làm*, vì mỗi
dòng phải khớp với một commit có thật.

## Quy ước ghi

- Ghi **ngay sau mỗi phiên làm việc có dùng AI**, không dồn.
- Mỗi dòng phải gắn được với commit tương ứng — chưa commit thì ghi
  `*(chưa commit)*` rồi cập nhật lại sau.
- **Không dán secret, khóa API, chuỗi kết nối hay dữ liệu người dùng thật vào
  prompt** — và cũng không dán chúng vào file này.
- Ghi **prompt ở dạng câu lệnh ngắn**, nêu đúng việc đã yêu cầu — không chép
  nguyên văn lời thoại.
- Ghi trung thực phần nào do AI sinh, phần nào do sinh viên tự viết hoặc sửa lại.

---

## 1. Nhật ký phiên làm việc

| Ngày | Công cụ | Phạm vi | Prompt chính | AI sinh ra | SV tự viết / sửa lại | Commit |
|---|---|---|---|---|---|---|
| 10/09/2026 | Claude Code (Opus 5) | Đọc rubric KLTN, rà soát repo, dựng khung tài liệu | Đọc repo và báo cáo TLCN. Đọc rubric KLTN. Dựng `CLAUDE.md`, `AGENTS.md`, `docs/` theo Spec Driven Development | Toàn bộ `AGENTS.md`, `CLAUDE.md`, `SRS.md`, `PLAN.md`, `DECISIONS.md` (D-001→D-010) và chính file này; xóa `.github/copilot-instructions.md` | SV định hướng và bác nhiều đề xuất trong phiên (§2); về sau sửa lại qua D-011, D-016, D-017, D-021 | *(chưa commit)* |
| 10/09/2026 | Claude Code (Opus 5) | Điều chỉnh backlog; tra rubric về P-03 và P-06 | Đổi tên P-01. Tra rubric xem P-03 là gì. Xét xem đồ án một người có cần PR. Gộp AC vào rà soát đặc tả. Tách SRS/SDD ngay từ đầu | Sửa `PLAN.md` (P-01, P-03, P-06, thêm P-07, gộp P-20 và P-22, bỏ P-27); thêm D-011, D-012, D-013; trích nguyên văn rubric để đối chiếu | SV chốt: vẫn dùng PR; tách SRS/SDD sớm (bác D-010 của AI); ghép khảo sát vào P-25 | *(chưa commit)* |
| 10/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Commit nhánh docs đầu tiên; đưa `.claude/` ra khỏi repo | Tạo nhánh đúng quy ước, push rồi mở PR. Gitignore `.claude/` | Hai commit docs tạo qua git worktree; sửa `.gitignore`; thêm D-014; tự phát hiện và sửa lỗi commit message (§2 #9) | SV chọn ignore `.claude/` thay vì commit ECC vào repo | `1ad9305`, `43c1a0a` |
| 10/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Soạn nháp bản cam kết sản phẩm (P-03) | Soạn nháp bản cam kết chức năng khi chưa có biểu mẫu và chưa có metric | `COMMITMENT.md`: 30 chức năng CN-01→CN-30 đối chiếu route/view trong code, 7 chỗ lệch L-1→L-7, trong đó L-6 là lỗi giá do trình duyệt gửi lên | SV duyệt danh sách 30 chức năng, chốt L-1 (D-015); các chỗ lệch còn lại để chốt sau | *(chưa commit)* |
| 11/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Chỉnh bản cam kết; chốt mô hình một nhà bán | Bỏ gợi ý sản phẩm nổi bật khỏi CN-17. Giữ mô hình một nhà bán | Thêm D-015; sửa `SRS.md` (A3 thành Nhân viên cửa hàng, 9 chỗ), `README.md`, `AGENTS.md`/`CLAUDE.md`, `PLAN.md` §6 | SV quyết mô hình một nhà bán và bỏ tính năng gợi ý nổi bật khỏi cam kết | *(chưa commit)* |
| 11/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Giải thích rồi gỡ công cụ AI `get_bestsellers` | Giải thích `get_bestsellers` làm gì. Tạm gỡ khỏi mã nguồn | Chỉ ra hàm không tính doanh số mà chỉ lọc cờ `featured`; diff xóa hàm khỏi `store_api/views.py` (−28 dòng), kiểm bằng `manage.py check`; sửa `SRS.md` §9.1 | SV quyết gỡ rồi đổi ý giữ lại; commit `7e96044` bỏ cùng nhánh local chưa từng push | — (đã hoàn nguyên) |
| 11/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Soạn đề cương KLTN theo khung đề cương TLCN | Đọc mục đề cương trong báo cáo TLCN, viết file tương đương cho KLTN | `DE_CUONG.md` 5 mục như bản TLCN, nội dung lấy từ `PLAN.md`, `COMMITMENT.md`, D-013, D-015; bố cục báo cáo thêm Chương 5 *Thực nghiệm người dùng* | SV yêu cầu sửa số người thực nghiệm về 3–5 và giữ chữ *chuyên ngành*; duyệt bản xuất 16/09, nộp portal 18/09 | `876df38` (PR #2) |
| 11/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Merge PR #2; đổi quy trình nhánh sang `develop` | Commit, push rồi merge vào `main`. Xóa nhánh vừa merge. Từ nay làm trên `develop` | Chạy checklist review rồi merge PR #2 bằng merge commit; tạo nhánh `develop`; thêm D-016 (thay D-012); sửa `PLAN.md`, `COMMITMENT.md` §5 | SV bỏ cách chia nhánh theo từng việc, chuyển sang làm trên `develop` | `87c1a2a` (PR #3) |
| 11–12/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | P-07 tách `SRS.md` / `SDD.md`; báo cáo Tuần 1; dựng phần nộp portal | Làm P-07 rồi viết `tuan-01.md`. Commit, mở PR, merge. Viết lại prompt trong nhật ký thành câu lệnh ngắn. Thêm phần tóm tắt portal và khai báo AI vào báo cáo tuần | Tạo `SDD.md` từ SRS Mục 7–9, `diff` xác nhận không đổi chữ nào; sửa `SRS.md` và các link liên quan; tạo `tuan-01.md`, `_TEMPLATE.md` §6–§7; viết lại cột prompt của 9 dòng nhật ký | SV bỏ ô *Hình thức trao đổi với GVHD* và mục *Cần GVHD quyết*; yêu cầu prompt dạng câu lệnh ngắn. AI tự quyết để Kiểm thử và Hạn chế ở lại SRS | `0af7a42`, `c5ed01c`, `7baa3e8` (PR #4); `c8159d6`, `4d58430` (PR #5) |
| 14/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Ghi nhận hạn nộp tuần và ý kiến GVHD Tuần 1 | Ghi hạn nộp trước 17:00 Thứ Sáu. Ghi ý kiến GVHD: lần sau nộp đề cương để góp ý | Thêm cột hạn nộp 15 tuần vào `weekly_report/README.md`; thêm P-08 và hạn nộp vào `PLAN.md`, `AGENTS.md`/`CLAUDE.md`, `DE_CUONG.md`. AI tự đặt quy ước: việc làm sau lúc nộp tính vào tuần kế tiếp | SV cung cấp hạn nộp và ý kiến GVHD; chốt định dạng đính kèm là PDF; duyệt quy ước trên (21/09) | `9e8dd3e` |
| 14/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Xuất đề cương sang DOCX/PDF theo mẫu TLCN (P-08) | So sánh xuất DOCX với PDF xem cái nào giống bản TLCN nhất. Cài pandoc rồi làm theo phương án đã đề xuất | Cài pandoc và LibreOffice; đo bố cục trang 6–9 của PDF TLCN; viết `export_de_cuong.py` và bộ lọc `de_cuong.lua`; xuất DOCX + PDF 7 trang. Không đụng mã nguồn ứng dụng, không sửa nội dung `DE_CUONG.md` | SV cung cấp link báo cáo TLCN, đồng ý cài pandoc, duyệt bản xuất; chốt giữ công cụ xuất ngoài repo | `9e8dd3e` (ghi chú P-08); công cụ và file xuất không vào repo |
| 15/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Sửa đề cương; chốt số người thực nghiệm (D-017) | Sửa phần người dùng thực tế thành khoảng 3–5 người. Đổi "ngành" thành "chuyên ngành". Xuất lại DOCX/PDF | Sửa `DE_CUONG.md` §1, §2.2, §5 và `PLAN.md` P-25; thêm D-017 kèm đính chính dưới D-013; xuất lại DOCX/PDF. Tra rubric TC2.7: 3–4 người là Mức 3, 5–9 là Mức 4, ≥ 10 là Mức 5 | SV chốt 3–5 người, giữ chữ "chuyên ngành"; SV tự viết lý do trong D-017: 3–5 là con số làm được, 10 là quá sức, ghi nhận đây là rủi ro chấp nhận | `9e8dd3e` |
| 16/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Phân biệt đề cương với bản cam kết; nháp báo cáo Tuần 2 | Tra rubric xem bản cam kết có phải đề cương không. Cập nhật PLAN rồi soạn nháp báo cáo Tuần 2 | Xếp ưu tiên việc trong tuần; đọc lại rubric Mục 2 và Mục 7: rubric không nhắc đề cương, bản cam kết cần metric, chữ ký và rà soát của bộ môn. Sửa `PLAN.md` (P-03, P-08, §5); nháp `tuan-02.md` | SV đặt câu hỏi đề cương và bản cam kết có phải một thứ không; duyệt bản xuất đề cương | `e056d7c` |
| 16/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | P-12: dựng hạ tầng kiểm thử; test đăng nhập, giỏ hàng, checkout, VNPay, trợ lý AI | Commit phần báo cáo tuần rồi làm P-12. Chia thành nhiều commit rồi push | Cài pytest, pytest-django, pytest-cov; tạo `settings_test.py`, `pytest.ini`, `conftest.py`, `requirements-dev.txt`, `.coveragerc`; viết 48 test (43 xanh, 5 `xfail`), độ phủ 66%. Phát hiện L-8, L-9, L-10 và hai thông báo TC_02, TC_04 không có trong code. Thêm D-018; sửa `COMMITMENT.md`, `SRS.md` §7, `PLAN.md`, `AGENTS.md`/`CLAUDE.md`. Không sửa dòng mã nguồn ứng dụng nào | SV yêu cầu làm P-12; yêu cầu chia commit theo nhóm và push lên `develop` | `d045ba7`, `f1417de`, `b8203bd`, `1467975`, `6df5e0f` |
| 17/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | P-10: dựng CI trên GitHub Actions; bổ sung migration thiếu từ TLCN | Làm P-10: dựng CI. Sinh migration `0005` | Chạy thử các lệnh định đưa vào CI, phát hiện `CartOrder` thiếu migration từ commit TLCN `51c4b96`; sinh `0005_remove_cartorder_stripe_payment_intent.py`. Tạo `.github/workflows/ci.yml` với job `twin-files` và job `test`. Kiểm lại trên bản sao không có `.env`: 43 passed, 5 xfailed, 66% | SV chọn sinh migration thay vì để CI đỏ hoặc bỏ chặng kiểm tra; yêu cầu commit và push | `5198f39` (migration), `717f4b8` (CI), commit tài liệu ngay sau |
| 18/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Theo dõi lần chạy CI đầu tiên; mở và merge PR #7; cập nhật báo cáo Tuần 2 | Commit và push. Merge vào main rồi cập nhật lại báo cáo tuần | Chia 3 commit (`fix:` migration, `ci:` workflow, `docs:` tài liệu); theo dõi CI lần đầu, xanh; mở PR #7 kèm checklist tự review, quét diff bằng regex tìm secret (chỉ khớp secret VNPay giả trong `settings_test.py`); cập nhật `tuan-02.md` | SV hỏi lại CI có phải chờ tách `.env` mới chạy được không; yêu cầu merge và cập nhật báo cáo | `5198f39`, `717f4b8`, `cc98448` |
| 21/09/2026 | Claude Code (Opus 5) | Tra rubric về nơi lưu báo cáo tuần; gỡ `weekly_report/` khỏi repo (D-021) | Tra rubric xem có bắt báo cáo tuần nằm trong repo không. Gitignore cả thư mục, lý do là rút gọn repo | Tra rubric Mục 5, 7, 8: nguồn kiểm chứng tần suất báo cáo là sổ theo dõi của GVHD, repo chỉ cần đủ lịch sử commit. Gitignore `docs/weekly_report/`; chuyển lịch 15 tuần sang `PLAN.md` §2 và quy ước viết văn bản sang `AGENTS.md`/`CLAUDE.md` §3; thêm D-021 | SV bác lý do AI đưa ra để giữ báo cáo trong repo, yêu cầu kiểm chứng bằng rubric (lỗi #21); chốt gitignore cả thư mục và chốt lý do ghi trong D-021 | *(chưa commit)* |
| 21/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | P-17 sửa L-6 (giá do trình duyệt gửi lên) | Làm L-6. Sửa luôn phần JavaScript của chỗ đó | Sửa `core/views.py`: thêm `_refresh_cart` đọc giá từ `Product`, `add_to_cart` dựng dòng giỏ từ database, tiền tính bằng `Decimal`; gỡ `xfail` của test L-6, thêm 3 test. Rút `/add-to-cart/` còn `id` và `qty`, bỏ phần JavaScript gửi giá cùng 28 dòng input ẩn trong 5 template. Kiểm: 47 passed, 4 xfailed, `check` và `makemigrations --check` sạch. Thêm D-022; sửa `COMMITMENT.md`, `PLAN.md` (thêm P-17), `SRS.md` §6.1 | SV chọn L-6 làm việc đầu tiên của Tuần 3; yêu cầu dọn luôn phần JavaScript thay vì để lại như AI đề xuất | `7ea9380`, `a0cf37a`, `23e91e1` |
| 21/09/2026 | Claude Code (Opus 5) | P-17 sửa L-8 (trang hoàn tất tự xác nhận đơn đã thanh toán) | Làm L-8 | Sửa `core/views.py`: `payment_completed_view` bỏ đoạn đặt `paid_status = True`, đơn online chưa được VNPay xác nhận thì quay về `core:checkout` kèm nhắc thanh toán; gỡ `xfail` của test L-8, thêm 2 test cho đơn online đã xác nhận và đơn COD. Kiểm: 50 passed, 3 xfailed, `check` và `makemigrations --check` sạch. Thêm D-023; sửa `COMMITMENT.md`, `PLAN.md`, `SRS.md` §6.1 | SV chốt làm L-8 | `8e22574` |
| 21/09/2026 | Claude Code (Opus 5) | P-17 sửa L-7 (giỏ hàng không kiểm tra tồn kho) | Làm L-7 | Sửa `core/views.py`: `_refresh_cart` đọc thêm `stock_count`, hạ số lượng vượt tồn kho xuống bằng tồn kho kèm `messages.warning`, bỏ dòng hết hàng; `add_to_cart` gọi `_refresh_cart` trước khi trả JSON. Gỡ `xfail` của test L-7, thêm 5 test ca biên. Kiểm: 56 passed, 2 xfailed. Thêm D-024; sửa `COMMITMENT.md`, `PLAN.md`, `SRS.md` §6.1 | SV chốt làm L-7 | `219b569` |
| 22/09/2026 | Claude Code (Opus 5) | P-17 sửa L-10 và L-9 (trợ lý AI thấy sản phẩm đã gỡ bán, không kiểm tra tồn kho) | Làm L-9 và L-10 | Sửa `store_api/views.py`: thêm `published_products()` và `buyable_products()`, ba câu truy vấn của công cụ AI và API đổi sang dùng hai hàm này thay cho `status=True, in_stock=True`; nhánh `request_add_to_cart` tách hai lý do `Product not found` và `Product is out of stock`. Gỡ `xfail` của L-9 và L-10, thêm 2 test cho công cụ AI và file mới `store_api/tests/test_product_api.py` với 4 test. Ghi chú hai cờ không dùng tại `core/models.py`. Kiểm: 64 passed, 0 xfailed, `makemigrations --check` sạch. Thêm D-025; sửa `COMMITMENT.md`, `PLAN.md`, `SRS.md` §6.1 | SV chốt làm L-9 và L-10 | `6b7dec8` |
| 23/09/2026 | Claude Code (Opus 5) | P-17 sửa L-5 và L-3 (đổi trạng thái đơn đã giao, đánh giá sản phẩm chưa mua); chốt L-2 và L-4 | Làm L-5 rồi L-3 | Tạo `useradmin/tests/` (conftest + 6 test cho UC-20) và `core/tests/test_reviews.py` (9 test cho UC-14), chạy cho đỏ trước khi sửa. Sửa `useradmin/views.py`: `change_order_status` từ chối trạng thái ngoài `STATUS_CHOICES` và chặn mọi thay đổi trên đơn đã `delivered`; đổi `<option value="pending">` trong `order_detail.html` thành `value=""`. Sửa `core/views.py`: thêm `has_received_product`, `ajax_add_review` có `@login_required` và ba nhánh từ chối, `make_review` dùng chung hàm đó. Kiểm: 79 passed, 0 xfailed, độ phủ 66% → 71%. Thêm D-026, D-027, D-028; sửa `SRS.md` §4.4 và §6.1, `COMMITMENT.md` §2 và §3, `PLAN.md` §4–§6 | SV chốt thứ tự L-5 trước L-3 và duyệt việc chốt L-2, L-4 bằng đường sửa đặc tả | `9316171`, `2d0bf9c` |
| 24/09/2026 | Claude Code (Opus 5) | P-15 quét secret; P-14 lint, phân tích tĩnh, trùng lặp | Làm P-15 và P-14. Dùng Ruff làm công cụ phân tích tĩnh. Sửa 30 lỗi lint, viết quy đổi Blocker/Critical, chốt cách đo trùng lặp. Xóa 13 template không dùng | Tạo `ruff.toml`, `.jscpd.json`; thêm job `lint` và `secret-scan` vào `ci.yml`. Quét `origin`: 0 secret. Sửa 30 lỗi lint, thêm 2 test; trùng lặp Python 1,09%. Xóa 13 template mẫu của theme, template còn trùng 26,6%. Thêm D-029, D-030, D-031, `COMMITMENT.md` §5.1; sửa `PLAN.md`, `AGENTS.md`/`CLAUDE.md` | SV đọc lại rubric, chốt Ruff làm công cụ phân tích tĩnh (lỗi #27), không bật `PLE`; giao AI sửa lỗi lint và soạn §5.1; quyết xóa 13 template | `ddf60cd` (gitleaks), `61dd8b2` (Ruff và bản sửa), `8326ea4` (jscpd), `d0021ff` (xóa template), commit tài liệu ngay sau |

---

## 2. Lỗi / ảo giác của AI đã phát hiện

Mức 5 cần **≥ 5 mục** kèm phân tích nguyên nhân và commit sửa. Hiện có: **28**.

| # | Ngày | Lỗi | Nguyên nhân | Ai phát hiện | Commit sửa |
|---|---|---|---|---|---|
| 1 | 10/09/2026 | AI viết `AGENTS.md` / `CLAUDE.md` bằng tiếng Anh trong khi `docs/` viết tiếng Việt | AI áp mặc định "tài liệu kỹ thuật thì viết tiếng Anh" thay vì theo ngữ cảnh đồ án tiếng Việt đã thiết lập | SV | *(chưa commit)* |
| 2 | 10/09/2026 | AI thiết kế `CLAUDE.md` chỉ là con trỏ tới `AGENTS.md` (D-003) | AI tối ưu cho mục tiêu "tránh trùng lặp" mà không hỏi yêu cầu thật; kết quả là `CLAUDE.md` đọc một mình thì vô nghĩa | SV | *(chưa commit)* — thay bằng D-006 |
| 3 | 10/09/2026 | AI chốt đặc tả TLCN là "baseline đóng băng", chỉ cho ghi delta (D-004) | AI giả định báo cáo đã chấm điểm thì bất biến, bỏ qua khả năng chính đặc tả đó có chỗ sai/lỗi thời | SV | *(chưa commit)* — thay bằng D-007 |
| 4 | 10/09/2026 | Script kiểm tra link Markdown báo 4 link "GÃY" trong khi cả 4 đều tồn tại | Regex cắt chuỗi không loại bỏ dấu `)` cuối, làm tên file thành `PLAN.md)` — lỗi false positive, suýt dẫn tới "sửa" thứ không hỏng | AI (tự phát hiện khi đối chiếu với `ls`) | *(chưa commit)* |
| 5 | 10/09/2026 | AI đặt tên tài liệu đặc tả là `PRD.md` | AI dùng thuật ngữ phổ biến trong ngành phần mềm thương mại, không đối chiếu Mục 8 của rubric — nơi gọi đích danh hồ sơ là "SRS/SDD" | SV | *(chưa commit)* — xem D-009 |
| 6 | 10/09/2026 | AI đặt tên P-01 là "Khung SDD" trong khi chính AI dùng "SDD" cho tài liệu thiết kế `SDD.md` (D-010) | AI dùng một từ viết tắt cho hai nghĩa (Spec Driven Development và Software Design Document) mà không kiểm tra xung đột trong cùng bộ tài liệu | SV | *(chưa commit)* — đổi thành "Khung specs" |
| 7 | 10/09/2026 | AI chốt hoãn tách SRS/SDD tới sau P-24 (D-010) | AI chỉ cân nhắc công sức tách file, bỏ qua việc tách muộn làm lịch sử Git của phần thiết kế bị cắt ở ngày tách — mất dấu vết quá trình rà soát, vốn là minh chứng rubric chấm | SV | *(chưa commit)* — thay bằng D-011 |
| 8 | 10/09/2026 | AI tách "viết acceptance criteria" (P-20) và "rà soát đặc tả TLCN" (P-23) thành hai việc riêng | AI chia việc theo loại sản phẩm đầu ra thay vì theo cách làm thực tế: cả hai đều phải đi qua cùng 26 use case, tách ra là rà hai lượt | SV | *(chưa commit)* — gộp vào P-23 |
| 9 | 10/09/2026 | AI tạo 2 commit mất dòng tiêu đề `docs: ...` trong commit message | AI nối heredoc với `&& \` trong bash, khiến dòng đầu của heredoc bị hiểu thành một lệnh riêng (`docs:: command not found`) thay vì thành nội dung message | AI (tự phát hiện qua output lỗi, làm lại trước khi push) | `1ad9305`, `43c1a0a` (bản đúng) |
| 10 | 10/09/2026 | Bản nháp đầu của `COMMITMENT.md` ghi CN-08 "không cho vượt tồn kho" và CN-19 "giỏ trống thì AI nhắc" là `Có` | AI chép hành vi từ đặc tả TLCN sang cột hiện trạng, chỉ kiểm tra route tồn tại chứ chưa đọc view. Đọc code thì thấy server không kiểm tra tồn kho (L-7) và phát hiện luôn lỗi giá do trình duyệt gửi lên (L-6) | AI (tự phát hiện khi kiểm chứng trước lúc ghi file) | *(chưa commit)* |
| 11 | 11/09/2026 | `AGENTS.md`/`CLAUDE.md` §3 ghi "Viết test kèm theo (xem §7)" trong khi mục Kiểm thử là §8 (§7 là Quy ước code) | AI viết tham chiếu chéo bằng số mục mà không đối chiếu với tiêu đề thật — sai ngay từ bản commit đầu tiên (`43c1a0a`). Tồn tại qua 3 PR vì checklist review chỉ kiểm tra hai file song sinh khớp *nhau*, không kiểm tra tham chiếu nội bộ có trỏ *đúng* hay không | AI (tự phát hiện khi sửa §3 cho P-07) | `0af7a42` |
| 12 | 12/09/2026 | AI tự ý bỏ hết dấu tiếng Việt trong khối tóm tắt dán lên portal | AI suy đoán portal sẽ lỗi phông khi dán tiếng Việt có dấu, trong khi portal là trang tiếng Việt của khoa — tự đặt ra một ràng buộc không có căn cứ rồi làm hỏng chất lượng văn bản theo nó | SV | `c8159d6` |
| 13 | 12/09/2026 | AI kết luận lỗi "No diagram type detected" của markdown preview là do chuỗi `markdown` sau dấu fence, sửa xong vẫn lỗi | AI đọc mã của extension thấy mermaid chỉ chạy trên phần tử `.mermaid` nhưng vẫn chốt nguyên nhân khi chưa kiểm chứng được; đúng ra phải nói rõ là giả thuyết. Cách sửa cuối cùng là bỏ hẳn khối code ở mục đó — né vấn đề, không phải giải quyết | SV (báo "vẫn bị") | `c8159d6` |
| 14 | 14/09/2026 | Khi ô bảng kế hoạch trong bản xuất đề cương không nhận style riêng, AI kết luận bộ lọc Lua sửa ô "tại chỗ" thì pandoc không ghi nhận, rồi viết lại đoạn code gán ngược từng tầng kèm chú thích khẳng định điều đó — xuất lại vẫn lỗi | AI chốt giả thuyết chỉ vì độ rộng cột đổi được còn style ô thì không, chưa kiểm chứng. Xuất cây AST (`pandoc -t native`) mới thấy Div đã nằm trong ô; nguyên nhân thật là pandoc luôn gán style `Compact` cho khối `Plain` trong bảng. Sửa bằng cách đổi nội dung ô thành `Para`, bỏ đoạn gán ngược và chú thích sai | AI (tự phát hiện khi kiểm chứng bằng AST) | — công cụ xuất đang ở thư mục tạm, chưa vào repo |
| 15 | 14/09/2026 | Dòng "Tp. Hồ Chí Minh, ngày … tháng … năm 2026" trong bản xuất bị dạt về lề trái thay vì nằm trên cột "Người viết đề cương" | AI dùng chung hai tab stop căn giữa của khung ký tên cho dòng ngày, trong khi dòng này chỉ có một ký tự tab — tab rơi vào tab stop của cột trái | AI (so ảnh bản xuất với trang TLCN) | — như trên |
| 16 | 14/09/2026 | Style "Outline Part" in đậm cả dòng, làm "5. PHỤ LỤC — bản cam kết sản phẩm và metric, …" đậm toàn bộ | AI suy từ bản TLCN (mọi dòng phần đều đậm cả dòng) mà không đối chiếu Markdown, nơi chỉ tên phần được đánh dấu `**…**` | AI (so ảnh bản xuất với trang TLCN) | — như trên |
| 17 | 15/09/2026 | D-013 (viết 10/09/2026) ghi TC2.7 "bắt buộc phải có ≥ 10 người dùng thật"; con số này lan sang đề cương (§1, §2.2, bảng kế hoạch) và P-25 | AI lấy ngưỡng Mức 5 của TC2.7 làm điều kiện bắt buộc, không phân biệt các mức (rubric Mục 5: ≥ 3 người là Mức 3, ≥ 5 là Mức 4) và không ghi rằng ngưỡng được điều chỉnh khi chốt bản cam kết | AI (đọc lại rubric khi SV đổi số người thành 3–5) | `9e8dd3e` — ghi chú đính chính dưới D-013, thêm D-017 |
| 18 | 16/09/2026 | Script Python sửa một lượt nhiều tài liệu (`SRS.md`, `PLAN.md`, `AGENTS.md`/`CLAUDE.md`) dừng giữa chừng với `ValueError`, sau khi đã ghi xong `SRS.md` và một phần `PLAN.md` | AI giả định dòng cuối file luôn có ký tự xuống dòng khi tìm chỗ chèn (`s.index("\n", ...)`), trong khi dòng 4 của bảng `PLAN.md` §6 là dòng cuối file và không có. Script lại ghi từng file ngay sau khi sửa, nên lỗi để lại trạng thái sửa dở. AI xem `git diff` để biết phần nào đã ghi rồi làm nốt bằng công cụ Edit; script sau giữ nguyên kiểu xuống dòng và báo rõ chuỗi không khớp | AI (qua output lỗi) | `d045ba7` (`AGENTS.md`/`CLAUDE.md`), `6df5e0f` (`SRS.md`, `PLAN.md`) |
| 19 | 16/09/2026 | Dòng "Tuần 2" của nhật ký tiến độ bị chèn vào bảng mốc thời gian `PLAN.md` §2 (giữa "Bắt đầu" và "Hết Giai đoạn 0") thay vì bảng Nhật ký tiến độ §5; lỗi nằm trong commit `e056d7c` và đã được push | Script tìm chỗ chèn bằng chuỗi `| Tuần 1 |` và lấy vị trí khớp **đầu tiên** mà không kiểm tra chuỗi có duy nhất hay không — chuỗi này khớp cả dòng `| Bắt đầu | Tuần 1 |` ở §2. Sau khi chèn, AI chỉ xem `git diff --stat` (số dòng đổi) chứ không đọc lại vị trí dòng mới, nên lỗi lọt qua 6 commit | AI (đọc lại `PLAN.md` sau khi push) | commit ngay sau `e3473d6`: *docs: move week 2 log row to PLAN section 5* |
| 20 | 17/09/2026 | Khi dựng CI, AI báo với SV rằng `.coveragerc` "chưa bao giờ được commit vì `.gitignore` chặn nó", nên CI sẽ đo độ phủ sai phạm vi — và định sửa `.gitignore`. Thực tế file đã được commit từ P-12 | AI chạy `cat .gitignore | head -60; ls -a grocerly | grep -i cov` trong **một** lệnh; dòng `.coveragerc` in ra từ lệnh `ls` nối ngay sau nội dung `.gitignore`, AI đọc thành một dòng của `.gitignore` rồi kết luận khi chưa kiểm chứng. Chạy `git check-ignore -v` (không trả gì) và `git ls-files` (có file) mới thấy sai. Bài học: gộp nhiều lệnh thì output phải có dấu phân cách; kết luận về trạng thái Git thì kiểm bằng lệnh Git | AI (tự phát hiện trước khi sửa) | — không có gì phải sửa; đã báo lại SV trong phiên |
| 21 | 21/09/2026 | AI khuyên không nên gỡ báo cáo tuần khỏi repo, viện lý do "phụ lục quyển báo cáo cần sổ theo dõi tiến độ 15 tuần" như thể đó là yêu cầu của rubric. Rubric không có yêu cầu nào về phụ lục của quyển báo cáo | AI lấy mục PHỤ LỤC trong `DE_CUONG.md` - văn bản do chính đồ án tự soạn - rồi trình bày nó như ràng buộc từ rubric, không đối chiếu lại bản rubric vừa tải về trong cùng phiên. Lẫn giữa nguồn quy định và nguồn tự khai | SV (hỏi lại "rubric có nhắc tới không") | D-021 |
| 22 | 21/09/2026 | Commit `a18a5ae` có dòng tiêu đề là đúng một ký tự `@`, tiêu đề thật tụt xuống dòng hai | AI soạn commit message bằng cú pháp here-string của PowerShell (`@'...'@`) nhưng chạy lệnh qua Bash. Bash không có here-string kiểu đó: nó đọc `@'` thành ký tự `@` rồi mới mở chuỗi nháy đơn, nên `@` thành dòng đầu của message. Cùng họ với mục #9 — hỏng commit message vì cú pháp shell — lần này do phiên làm việc có cả hai shell và AI lấy mẫu của shell này chạy bằng shell kia. Hai commit sau đó AI đổi sang `git commit -F -` với heredoc `<<'EOF'` thì đúng | SV (hỏi lại "dấu `@` đâu ra") | `401f3d7` — viết lại message bằng `commit --amend` trên HEAD tách rời rồi `rebase --onto`; hai commit nằm trên replay thành `4659aae`, `267c326`. Nội dung file không đổi, kiểm bằng `git diff` |
| 23 | 21/09/2026 | Docstring của `_refresh_cart` do AI viết khẳng định ô nhập số lượng trên trang sản phẩm cũng có chặn trên, trong khi template chỉ đặt `min="1"` | AI suy ra từ thói quen "ô number thì có min và max" rồi viết thẳng vào tài liệu của hàm, không mở template ra xem. Loại lỗi nguy hiểm vì nó nằm trong comment: test không bắt được, người đọc sau này lại tin | AI (tự phát hiện khi grep lại template để kiểm câu vừa viết) | `219b569` |
| 24 | 22/09/2026 | Test `test_search_tool_returns_only_products_in_stock` do AI viết ở P-12 vẫn xanh nhưng không kiểm đúng thứ nó đặt tên: sản phẩm "hết hàng" trong test được tạo bằng cờ `in_stock=False` và để `product_status` mặc định `in_review`, nên nó bị loại vì lý do nào cũng được. Test `test_search_tool_returns_at_most_five_products` cũng tạo sản phẩm `in_review` và chỉ xanh nhờ câu lọc cũ bỏ qua cột đó | AI viết test bằng cách đọc câu lọc trong code rồi dựng dữ liệu vừa khít câu lọc đó, thay vì dựng dữ liệu theo yêu cầu trong `SRS.md`. Test kiểu này chép lại lỗi của code nên không bao giờ đỏ - đúng loại điểm mù mà rubric nhắm tới khi đòi test có ca âm. Chỉ lộ ra khi sửa L-10 làm câu lọc đổi và hai test vẫn xanh dù lý do đã khác hẳn | AI (tự phát hiện khi rà lại test nào sẽ đỏ sau khi đổi câu lọc) | `6b7dec8` — sửa dữ liệu đầu vào của cả hai test |
| 25 | 23/09/2026 | Đoạn tổng kết §3 của `COMMITMENT.md` do AI viết ghi "Bốn chỗ chốt bằng cách sửa đặc tả" rồi liệt kê ba mã, "Sáu chỗ còn lại sửa code" rồi liệt kê bảy | AI viết câu tổng kết dựa theo đoạn cũ ("thêm 2 lần sửa đặc tả và 2 lần sửa code") thay vì đếm lại bảng — cộng số cũ với số mới trong đầu chứ không tra nguồn. Loại lỗi nặng với văn bản sẽ đem ký: bản thân bảng đúng, chỉ câu tóm tắt tự mâu thuẫn với nó, mà người đọc nhanh thì đọc đúng câu tóm tắt | AI (tự phát hiện khi đọc lại đoạn vừa ghi) | sửa trong cùng phiên, trước khi commit |
| 26 | 24/09/2026 | AI chạy Ruff không kèm cấu hình và gọi đó là bộ luật mặc định `E4, E7, E9, F`, trong khi kết quả ra cả luật RUF, UP, SIM | AI dựa vào hiểu biết về các bản Ruff cũ, không kiểm tra bản 0.16.8 vừa cài đã nới bộ mặc định. Nếu không để ý, con số 93 lỗi sẽ bị ghi như số lỗi theo luật cơ bản | AI (đọc tên luật trong output) | sửa trong cùng phiên; `ruff.toml` ghi rõ từng nhóm luật (D-029) |
| 27 | 24/09/2026 | AI ghi vào D-029 rằng Ruff không đo được số Blocker/Critical, và nói với SV rằng chỉ dùng Ruff thì phần đó dừng quanh Mức 3, trong khi lại đề xuất coi loại E/F của Pylint là Blocker/Critical | AI coi Blocker/Critical là tính năng của công cụ chứ không phải một thang rubric để ngỏ, nên áp hai chuẩn khác nhau cho Pylint và Ruff; không đọc lại danh sách công cụ trong rubric, nơi ESLint chỉ có error và warn | SV (đọc lại rubric, chỉ ra Pylint có trong danh sách) | sửa trong cùng phiên, trước khi commit (D-029) |
| 28 | 24/09/2026 | Khi chạy thử chế độ Cloudinary, AI đặt `CLOUDINARY_URL` giả và tin rằng `.env` không ghi đè được; thực tế lần chạy đó nạp cấu hình Cloudinary thật của TLCN vào bộ nhớ (không gọi mạng, không in giá trị) | AI chỉ nghĩ tới `load_dotenv`, vốn không ghi đè biến có sẵn, mà không đọc lại `settings.py`: ngay dưới đó có vòng lặp tự chép `.env` vào `os.environ`, điều docstring của `settings_test.py` đã ghi | AI (kết quả kiểm tra ra False) | chạy lại trên bản clone không có `.env`; repo không có gì phải sửa |

**Ghi chú khi bảo vệ:** mười hai trong hai mươi tám mục trên do sinh viên phát hiện, không phải
AI tự sửa. Đó là điểm cần nói thẳng — nó cho thấy vai trò kiểm soát nằm ở người,
đúng tinh thần TC2.3.

---

## 3. Quy trình kiểm soát đầu ra AI

Mức 5 yêu cầu việc kiểm soát phải **thành hệ thống**, không phải "chạy thử thấy
được thì thôi". Checklist áp dụng cho mọi output của AI trước khi commit:

- [ ] **Đọc hết** phần AI sinh ra. Không commit thứ chưa đọc.
- [ ] **Đối chiếu tài liệu chính thức** (Django docs, docs của thư viện, rubric)
      thay vì tin lời AI — đặc biệt với tên API, tham số, và con số ngưỡng.
- [ ] **Chạy test và linter**, không chỉ chạy thử bằng tay.
- [ ] **Kiểm tra prompt không chứa** secret, khóa API, chuỗi kết nối, dữ liệu
      người dùng thật.
- [ ] **Kiểm tra giấy phép** nếu AI đề xuất chép đoạn mã hoặc thêm thư viện ngoài.
- [ ] **Câu hỏi cuối cùng:** *nếu hội đồng chỉ vào đúng dòng này và cho 3 phút,
      mình có giải thích được nó làm gì, vì sao có mặt ở đây, và bỏ đi thì hệ
      thống hỏng chỗ nào không?* Nếu không → chưa được commit.

---

## Liên kết

- Kế hoạch và backlog: [`PLAN.md`](PLAN.md) — P-04
- Quyết định quan trọng: [`DECISIONS.md`](DECISIONS.md)
- Đặc tả yêu cầu: [`SRS.md`](SRS.md)
- Đặc tả thiết kế: [`SDD.md`](SDD.md)
- Báo cáo tuần: trên portal của trường (không lưu trong repo — D-021)

# AI USAGE LOG — Grocerly (KLTN)

Nhật ký sử dụng LLM / Agentic AI trong quá trình làm đồ án.

| | |
|---|---|
| Sinh viên | Lê Đức Phát — 21139083 |
| Bắt đầu ghi | 10/09/2026 |
| Phục vụ tiêu chí | TC2.3 (5 điểm) — *Mức độ làm chủ và năng lực kiểm soát khi sử dụng LLM / Agentic AI* |

---

## Vì sao có file này

Rubric KLTN nói rõ: **dùng AI không bị trừ điểm**. Thứ bị chấm là mức độ sinh viên
làm chủ sản phẩm. Cụ thể:

- Không nộp nhật ký này (và cũng không nộp bản cam kết không dùng AI) → gate **G3**
  → TC2.3 bị chặn ở **2,75/5**.
- Nội dung kê khai mâu thuẫn với lịch sử Git hoặc với phần trả lời vấn đáp → gate
  **G4** → TC2.3 = **0 điểm**, đồng thời kích hoạt quy trình xác định đạo văn.
- Ngưỡng Mức 5: nhật ký **đối chiếu được với lịch sử commit**, giải thích đúng
  **100%** vị trí mã do AI sinh khi hội đồng chọn ngẫu nhiên ≥ 5 chỗ, và nêu được
  **≥ 5 lỗi/ảo giác của AI đã tự phát hiện** kèm phân tích nguyên nhân và commit sửa.

Nói cách khác: file này không phải thủ tục hành chính. Nó là bằng chứng cho câu
"em hiểu code của em", và nó chỉ có giá trị nếu được ghi *ngay khi làm*, vì mỗi
dòng phải khớp với một commit có thật.

## Quy ước ghi

- Ghi **ngay sau mỗi phiên làm việc có dùng AI**, không dồn.
- Mỗi dòng phải gắn được với commit tương ứng — chưa commit thì ghi
  `*(chưa commit)*` rồi cập nhật lại sau.
- **Không dán secret, khóa API, chuỗi kết nối hay dữ liệu người dùng thật vào
  prompt** — và cũng không dán chúng vào file này.
- Ghi **prompt ở dạng câu lệnh ngắn**, nêu đúng việc đã yêu cầu — không chép
  nguyên văn lời thoại.
- Ghi trung thực phần nào do AI sinh, phần nào do sinh viên tự viết hoặc sửa lại.

---

## 1. Nhật ký phiên làm việc

| Ngày | Công cụ | Phạm vi | Prompt chính | AI sinh ra | SV tự viết / sửa lại | Commit |
|---|---|---|---|---|---|---|
| 10/09/2026 | Claude Code (Opus 5) | Đọc rubric KLTN; rà soát repo đối chiếu rubric; dựng khung tài liệu theo Spec Driven Development | Đọc repo và báo cáo TLCN để nắm bối cảnh. Đọc rubric KLTN mới nhất. Dựng `CLAUDE.md`, `AGENTS.md` và `docs/` (PLAN, DECISIONS, PRD, weekly_report) theo Spec Driven Development | **Toàn bộ** nội dung `AGENTS.md`, `CLAUDE.md`, `docs/SRS.md`, `docs/PLAN.md`, `docs/DECISIONS.md` (D-001→D-010), `docs/weekly_report/README.md` + `_TEMPLATE.md`, và chính file này. Xóa `.github/copilot-instructions.md`. | SV định hướng và bác bỏ nhiều đề xuất của AI trong phiên (xem §2); tại thời điểm này chưa tự viết lại nội dung. Về sau năm file khung đi qua nhiều lượt SV quyết và bác bỏ: D-011 (tách SRS/SDD sớm), D-016 (làm trên `develop`), D-017 (3–5 người), D-021 (gỡ báo cáo tuần khỏi repo) | *(chưa commit)* |
| 10/09/2026 | Claude Code (Opus 5) | Điều chỉnh backlog `PLAN.md`; hỏi rubric về P-03 (bản cam kết) và P-06 (PR khi làm một mình) | Đổi tên P-01 thành Khung specs. Tra rubric xem P-03 là gì và nằm ở đâu. Xét xem đồ án một người có cần nhánh + PR (P-06). Gộp việc viết acceptance criteria vào rà soát đặc tả. Tách SRS/SDD ngay từ đầu. Gộp P-22 vào P-25 | Sửa `PLAN.md` (P-01, P-03, P-06, thêm P-07, gộp P-20 vào P-23, gộp P-22 vào P-25, bỏ P-27); thêm D-011, D-012, D-013 vào `DECISIONS.md`; sửa ghi chú ranh giới ở đầu `SRS.md`. AI đọc nguyên văn rubric trên Google Drive để trích dẫn; AI đề xuất phương án ghép khảo sát vào thực nghiệm | SV quyết định: vẫn chia nhánh + tự PR; gộp AC vào rà soát; tách SRS/SDD sớm (bác bỏ D-010 của AI); chọn ghép khảo sát vào P-25 | *(chưa commit)* |
| 10/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Commit, push nhánh docs đầu tiên; đưa `.claude/` ra khỏi repo | Tạo nhánh mới đặt tên đúng convention để commit, push rồi mở PR. Đưa `.claude/` vào `.gitignore` | Đặt tên nhánh `docs/kltn-specs-framework`; tạo 2 commit docs từ `main` qua git worktree; bỏ commit cài ECC chưa push; sửa `.gitignore`; thêm D-014. AI tự sửa lỗi commit message (xem §2 #9) | SV chọn ignore `.claude/` thay vì commit ECC vào repo | `1ad9305`, `43c1a0a` |
| 10/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Soạn nháp bản cam kết sản phẩm P-03 | Soạn nháp bản cam kết các chức năng của Grocerly để xem trước, khi chưa có biểu mẫu và chưa có metric | `docs/COMMITMENT.md`: 30 chức năng (CN-01→CN-30) đối chiếu với route/view trong code; 7 chỗ lệch L-1→L-7, trong đó L-6 là lỗi giá do trình duyệt gửi lên. AI đọc `urls.py`, `core/views.py`, `useradmin/views.py`, `store_api/views.py`, `core/admin.py` để đối chiếu | Chờ SV duyệt danh sách và chốt từng chỗ lệch | *(chưa commit)* |
| 11/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Chỉnh bản cam kết; chốt mô hình một nhà bán | Bỏ gợi ý sản phẩm nổi bật khỏi CN-17. Giữ mô hình một nhà bán, không phải multi-vendor | Bỏ "gợi ý sản phẩm nổi bật" khỏi CN-17; thêm D-015; sửa `SRS.md` (A3 → Nhân viên cửa hàng, 9 chỗ), `README.md` (bỏ "Multi-Vendor"), `AGENTS.md`/`CLAUDE.md` (§5, §6); ghi `PLAN.md` §6 dòng 3 | SV quyết định: mô hình một nhà bán; bỏ tính năng gợi ý nổi bật khỏi cam kết | *(chưa commit)* |
| 11/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Giải thích rồi gỡ công cụ AI `get_bestsellers` | Giải thích `get_bestsellers` làm gì và tính toán thế nào. Tạm gỡ công cụ này khỏi mã nguồn | AI đọc code và giải thích: hàm không tính doanh số, chỉ lọc cờ `featured`. AI sinh diff xóa hàm, bỏ khỏi `tools`, xóa nhánh xử lý trong `ai_chat` (`store_api/views.py`, −28 dòng); kiểm bằng `py_compile` + `manage.py check`. Sửa `SRS.md` §9.1 | SV quyết định gỡ, sau đó đổi ý: **tạm giữ lại** trong code. Commit gỡ `7e96044` bị bỏ cùng nhánh local `fix/remove-bestsellers-tool` (chưa từng push); `SRS.md` §9.1 hoàn nguyên về 4 công cụ | — (đã hoàn nguyên) |
| 11/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Soạn đề cương KLTN theo khung đề cương TLCN | Đọc mục ĐỀ CƯƠNG BÁO CÁO MÔN HỌC trong báo cáo TLCN, viết một file tương đương trong `docs/` cho KLTN | `docs/DE_CUONG.md`: giữ 5 mục như đề cương TLCN (tài liệu ban đầu, nhiệm vụ lý thuyết/thực hành, sản phẩm dự kiến, bố cục báo cáo, kế hoạch). Nội dung lấy từ `PLAN.md` (lịch, backlog P-xx), `COMMITMENT.md` (30 chức năng, các chỗ lệch L-x) và D-013, D-015; bố cục báo cáo thêm Chương 5 *Thực nghiệm người dùng* | SV duyệt sau khi yêu cầu sửa: số người thực nghiệm về 3–5 và giữ chữ *chuyên ngành* (15/09); duyệt bản xuất 16/09; nộp portal 18/09 | `876df38` (PR #2) |
| 11/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Commit, merge PR #2; đổi quy trình nhánh sang `develop` | Commit, push rồi merge vào `main`. Xóa nhánh vừa merge. Từ nay làm trên nhánh `develop` | Chạy checklist review (twin check, quét secret, file cấm) rồi tạo và merge PR #2 bằng merge commit; xóa nhánh `docs/product-commitment`; tạo nhánh `develop`. Thêm D-016 (thay D-012), cập nhật `PLAN.md` (P-06, nhật ký Tuần 1), `COMMITMENT.md` §5 | SV quyết định bỏ nhánh theo từng việc, chuyển sang làm trên `develop`. AI đề xuất ghi thành quyết định mới và giữ việc merge vào `main` qua PR | `87c1a2a` (PR #3) |
| 11–12/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | P-07 tách `SRS.md` / `SDD.md`; viết báo cáo Tuần 1; dựng phần nộp portal cho báo cáo tuần | Làm P-07 trước, xong thì viết báo cáo `tuan-01.md`. Commit và push. Mở PR rồi merge vào `main`. Viết lại prompt trong nhật ký AI thành câu lệnh ngắn. Thêm vào báo cáo tuần phần tóm tắt để dán lên portal (kèm link repo) và phần khai báo công cụ AI; ghi luật này lại cho các tuần sau. Bỏ mục *Cần GVHD quyết* | Tạo `docs/SDD.md`: chuyển nguyên văn SRS Mục 7–9 (đánh số lại 1–3), thêm phần đầu, ghi chú "nợ thiết kế", mục lịch sử chỉnh sửa. Sửa `SRS.md`: thay ghi chú "sẽ tách", đánh số lại Mục 10–12 → 7–9. Cập nhật link SDD ở `AGENTS.md`/`CLAUDE.md`, `PLAN.md` (P-05, P-06, P-07, §5, §6 dòng 4), `DECISIONS.md`, `weekly_report/README.md`, `_TEMPLATE.md`. Kiểm tra bằng `diff` rằng khối nội dung chuyển sang không đổi chữ nào. Tạo `weekly_report/tuan-01.md` từ `git log`, `gh pr list` và các file `docs/`. Viết lại cột *Prompt chính* của 9 dòng nhật ký thành câu lệnh ngắn. Thêm `tuan-01.md` §6 (bản tóm tắt dán vào ô *Nội dung* của portal, kèm link repo và link file trên GitHub) và §7 (khai báo công cụ AI: prompt / nội dung AI tạo ra / phần SV sửa); cùng khung tương ứng trong `_TEMPLATE.md`, luật trong `weekly_report/README.md` và `AGENTS.md`/`CLAUDE.md` §3 | SV bỏ ô *Hình thức trao đổi với GVHD* và bỏ hẳn mục *Cần GVHD quyết* khỏi báo cáo tuần; yêu cầu ghi prompt dạng câu lệnh ngắn thay vì chép nguyên văn. AI tự quyết để Kiểm thử và Hạn chế ở lại SRS (D-011 không nói tới hai mục này) | `0af7a42`, `c5ed01c`, `7baa3e8` (PR #4); `c8159d6`, `4d58430` (PR #5) |
| 14/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Ghi nhận hạn nộp báo cáo tuần và ý kiến GVHD cho bản nộp Tuần 1 | Ghi nhận hạn nộp báo cáo tuần trước 17:00 Thứ Sáu. Ghi ý kiến GVHD cho bản Tuần 1: lần sau nộp đề cương để góp ý nội dung | Thêm cột *Hạn nộp portal* (15 ngày Thứ Sáu) và hai quy tắc nộp vào `weekly_report/README.md`; điền mục *Ý kiến GVHD* cuối `tuan-01.md`; thêm P-08 (nộp đề cương) và hạn nộp vào `PLAN.md` §2, P-05; ghi hạn nộp vào `AGENTS.md`/`CLAUDE.md` §2, `DE_CUONG.md` (ghi chú đầu file, dòng P-05 ở Mục 5); `_TEMPLATE.md` thay ô *Hình thức trao đổi với GVHD* bằng ô *Nộp portal* kèm hạn. AI tự đặt quy ước: việc làm sau lúc nộp (tối Thứ Sáu → Chủ Nhật) tính vào báo cáo tuần kế tiếp | SV cung cấp hạn nộp và ý kiến GVHD; chốt định dạng file đính kèm là PDF (nộp 18/09). Còn treo: quy ước tính việc làm sau lúc nộp | `9e8dd3e` |
| 14/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Xuất đề cương KLTN sang DOCX/PDF giống trang đề cương của báo cáo TLCN để nộp portal (P-08) | So sánh xuất DOCX với PDF xem cái nào giống bản TLCN nhất. Cài pandoc rồi làm các bước đã đề xuất | Cài pandoc 3.11 (winget, phạm vi người dùng) và LibreOffice 26.8 (winget — AI tự chọn thay cho cách chuyển qua Google Docs); tải PDF TLCN từ Drive, cài PyMuPDF trong venv tạm để đo trang 6–9 (font, cỡ chữ, lề, thụt lề, đường kẻ bảng). Viết `export_de_cuong.py` (dựng `reference.docx` theo số đo, chạy pandoc, giữ hàng bảng không tách trang, gọi LibreOffice) và bộ lọc pandoc `de_cuong.lua` (đưa tên trường lên trên tiêu đề, bảng thông tin thành dòng "nhãn: giá trị", gạch đầu dòng, bố cục mục 4, khung ký tên hai cột; bỏ khung ghi chú nháp của repo; link tới file trong repo trỏ về GitHub). Xuất `DE_CUONG_KLTN_LEDUCPHAT.docx` và `.pdf` (7 trang). Không đụng mã nguồn ứng dụng, không sửa nội dung `DE_CUONG.md` | SV cung cấp link báo cáo TLCN và đồng ý cài pandoc; duyệt bản xuất 16/09. Còn treo: có đưa công cụ xuất vào repo hay không | `9e8dd3e` (ghi chú P-08); công cụ và file xuất vẫn ở thư mục tạm, chưa vào repo |
| 15/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Sửa đề cương theo yêu cầu SV; chốt số người thực nghiệm (D-017); xuất lại DOCX/PDF | Sửa mục Người dùng thực tế thành khoảng 3–5 người. Đổi "ngành" thành "chuyên ngành" Công nghệ Phần mềm. Sửa các chỗ còn lại thành 3–5 người; giữ chữ chuyên ngành như cũ | Sửa `DE_CUONG.md` §1 (2 dòng), §2.2 và bảng kế hoạch §5; `PLAN.md` P-25 và dòng quyết định liên quan; thêm D-017 và ghi chú đính chính dưới D-013 trong `DECISIONS.md`; xuất lại `DE_CUONG_KLTN_LEDUCPHAT.docx` và `.pdf`. AI đọc lại rubric (TC2.7 và Mục 5) và chỉ ra ngưỡng tham chiếu: 3–4 người là Mức 3, 5–9 người là Mức 4, ≥ 10 người là Mức 5, ngưỡng được phép điều chỉnh khi chốt bản cam kết. AI soạn phần lý do của D-017 từ câu trả lời "tầm 3–5 người thôi" của SV | SV chốt khoảng 3–5 người cho mọi chỗ trong đề cương và kế hoạch; chọn giữ chữ "chuyên ngành" ở đề cương, không đổi `AGENTS.md`/`CLAUDE.md`. SV sửa lý do trong D-017: 3–5 người là con số dự tính làm được, 10 người là hơi quá — ghi nhận là rủi ro chấp nhận (*accepted risk*) | `9e8dd3e` |
| 16/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Xác định việc kế tiếp theo PLAN; phân biệt đề cương với bản cam kết; cập nhật PLAN và soạn nháp báo cáo Tuần 2 | Theo PLAN thì việc kế tiếp nên làm là gì. Tra lại rubric xem bản cam kết sản phẩm có phải là đề cương không. Cập nhật PLAN rồi soạn nháp báo cáo Tuần 2 | AI đọc `PLAN.md` và xếp ưu tiên: nộp đề cương + `tuan-02.md` trước Thứ Sáu, sau đó P-12 rồi P-10, P-03 chạy nền. AI đọc lại nguyên văn rubric Mục 2 (Bước 1–4) và Mục 7 (G1): rubric không nhắc tới đề cương; bản cam kết là hồ sơ của Bước 3, cần metric, chữ ký SV + GVHD và rà soát của bộ môn. Sửa `PLAN.md` (P-03, P-08, §5 dòng Tuần 2); tạo nháp `weekly_report/tuan-02.md` từ `git log`, `gh pr list` và nhật ký này | SV duyệt bản DOCX/PDF của đề cương; đặt câu hỏi đề cương và bản cam kết có phải một thứ không; đồng ý để AI cập nhật PLAN và soạn nháp báo cáo | `e056d7c` |
| 16/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | P-12: dựng hạ tầng kiểm thử tự động; viết test cho đăng ký/đăng nhập, giỏ hàng, checkout, VNPay, trợ lý AI | Commit phần báo cáo tuần. Sau đó làm P-12. Chia phần P-12 thành nhiều commit rồi push | Commit `e056d7c`. Cài pytest 9.1.1, pytest-django 4.14.0, pytest-cov 7.1.0 vào `.venv`. Tạo `grocerly/grocerly/settings_test.py` (SQLite trong bộ nhớ, `InMemoryStorage`, secret VNPay giả, xóa trắng `GEMINI_API_KEY`), `pytest.ini` (`xfail_strict`), `conftest.py` (chốt chặn database + fixture dùng chung), `requirements-dev.txt`, `.coveragerc`; xóa 4 file `tests.py` rỗng. Viết 48 test: `userauths/tests/test_auth.py` (12), `core/tests/test_cart.py` (8), `test_checkout.py` (11), `test_payment.py` (5), `store_api/tests/test_ai_chat.py` (12) — 43 xanh, 5 `xfail`. AI tự kiểm chứng: chốt chặn dừng khi settings trỏ PostgreSQL (dùng settings giả trỏ cổng 1, không chạm database thật); bộ test xanh trên bản sao không có `.env`; chạy `--runxfail` để thấy từng test `xfail` hỏng đúng vì lỗi đã mô tả. Phát hiện 3 lỗi mới L-8 (mở trang hoàn tất là đơn online thành đã thanh toán), L-9 (AI thêm sản phẩm hết hàng), L-10 (AI tìm thấy sản phẩm đã ẩn) và 2 câu thông báo TC_02, TC_04 không có trong code. Thêm D-018; sửa `COMMITMENT.md` §2–§3, `SRS.md` §7, `PLAN.md` (P-12, P-13, §5, §6 dòng 5), `AGENTS.md`/`CLAUDE.md` §6, §8, §9, `tuan-02.md`. Đo độ phủ lần đầu: 66%. Sau khi agent review (không có lỗi CRITICAL/HIGH), sửa 2 góp ý LOW cho dễ giải thích trong `test_payment.py`. Chia thành 4 commit test + commit tài liệu, push `develop`. Không sửa dòng mã nguồn ứng dụng nào | SV yêu cầu làm P-12; sau đó yêu cầu chia thành nhiều commit theo nhóm và push lên `develop` | `d045ba7`, `f1417de`, `b8203bd`, `1467975`, `6df5e0f` |
| 17/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | P-10: dựng CI trên GitHub Actions; bổ sung migration còn thiếu từ TLCN | Làm P-10: dựng CI. Chọn cách xử lý model lệch migration: sinh migration `0005` | AI chạy thử trên máy các lệnh định đưa vào CI (`manage.py check`, `makemigrations --check --dry-run`, `pytest --cov`) với `settings_test`, phát hiện model `CartOrder` đã bỏ `stripe_payment_intent` từ commit TLCN `51c4b96` mà thiếu migration; truy nguồn bằng `git log -S`. Sau khi SV chọn phương án, sinh `core/migrations/0005_remove_cartorder_stripe_payment_intent.py` bằng `makemigrations` với `settings_test` (không kết nối database nào, không `migrate`). Tạo `.github/workflows/ci.yml`: job `twin-files` và job `test` (Python 3.12, cài `requirements-dev.txt` → `check` → `makemigrations --check` → `pytest --cov` → bảng độ phủ vào trang tóm tắt + artifact `coverage.xml`). AI tự kiểm chứng: chạy lại đúng các bước trên bản sao `git archive` không có `.env`, venv Python 3.12 mới — xanh, 43 passed + 5 xfailed, 66%. Thêm D-019, D-020; sửa `AGENTS.md`/`CLAUDE.md` §6, §8 (cùng một script, `diff` xác nhận khớp); `PLAN.md` (P-10, P-13, §5); `tuan-02.md`. Không sửa dòng mã nguồn ứng dụng nào | SV chọn sinh migration `0005` thay vì để CI đỏ hoặc bỏ chặng kiểm tra migration; yêu cầu commit và push | `5198f39` (migration), `717f4b8` (CI), commit tài liệu ngay sau: *docs: record P-10 CI setup (D-019, D-020)* |
| 18/09/2026 | Claude Code (Opus 5) + bộ công cụ ECC | Commit, push phần P-10; theo dõi lần chạy CI đầu tiên; mở PR #7 `develop` → `main`, merge; cập nhật báo cáo Tuần 2 | Commit và push. Merge vào main rồi cập nhật lại báo cáo tuần | Chia thành 3 commit (`fix:` migration, `ci:` workflow, `docs:` tài liệu), điền mã commit vào nhật ký này và `tuan-02.md`, push `develop`; theo dõi lần chạy CI đầu tiên (xanh). Mở PR #7 với checklist tự review; trước khi đánh dấu mục "không có chuỗi giống secret", quét diff bằng regex — chỉ khớp secret VNPay giả trong `settings_test.py`. Cập nhật `tuan-02.md` (ngày nộp, 14 commit, PR #7, 2 lần chạy CI đều xanh, chặng deploy chờ P-11), bỏ dòng đánh dấu bản nháp | SV hỏi lại CI có phải chờ tách `.env` mới chạy được không; AI giải thích CI không đọc `.env` (chạy `settings_test`), chỉ chặng deploy phải chờ P-11. SV yêu cầu merge và cập nhật báo cáo | `5198f39`, `717f4b8`, `cc98448`; commit báo cáo ngay sau: *docs: finalize week 2 report after PR #7* |
| 21/09/2026 | Claude Code (Opus 5) | Tra rubric về nơi lưu báo cáo tuần; đưa thư mục `weekly_report/` ra khỏi repo (D-021) | Tra rubric xem có bắt báo cáo tuần phải nằm trong repo không. Gitignore cả thư mục `weekly_report`, lý do là rút gọn repo vì báo cáo đã nộp trên portal | Tải rubric dạng `.txt` qua link export của Google Docs, tra Mục 5, 7, 8: nguồn kiểm chứng tần suất báo cáo là sổ theo dõi của GVHD, hồ sơ bắt buộc đòi sổ **có xác nhận của GVHD**, repository chỉ cần đầy đủ lịch sử commit. Thêm `docs/weekly_report/` vào `.gitignore`, gỡ thư mục khỏi vùng theo dõi (file vẫn còn trên đĩa và trong lịch sử Git). Chuyển lịch 15 tuần sang `PLAN.md` §2 và quy ước viết văn bản nộp GVHD sang `AGENTS.md`/`CLAUDE.md` §3. Thêm D-021; sửa link ở `DE_CUONG.md`, `PLAN.md`, `AI_USAGE_LOG.md`. Không sửa mã nguồn | SV bác lý do AI đưa ra để giữ báo cáo trong repo và yêu cầu kiểm chứng lại bằng rubric (lỗi #21). SV chốt gitignore cả thư mục thay vì chỉ các file `tuan-NN.md`, và chốt lý do ghi trong D-021 là rút gọn repo, bỏ phần nói về nhận xét của GVHD vì repo là public | *(chưa commit)* |

---

## 2. Lỗi / ảo giác của AI đã phát hiện

Mức 5 cần **≥ 5 mục** kèm phân tích nguyên nhân và commit sửa. Hiện có: **21**.

| # | Ngày | Lỗi | Nguyên nhân | Ai phát hiện | Commit sửa |
|---|---|---|---|---|---|
| 1 | 10/09/2026 | AI viết `AGENTS.md` / `CLAUDE.md` bằng tiếng Anh trong khi `docs/` viết tiếng Việt | AI áp mặc định "tài liệu kỹ thuật thì viết tiếng Anh" thay vì theo ngữ cảnh đồ án tiếng Việt đã thiết lập | SV | *(chưa commit)* |
| 2 | 10/09/2026 | AI thiết kế `CLAUDE.md` chỉ là con trỏ tới `AGENTS.md` (D-003) | AI tối ưu cho mục tiêu "tránh trùng lặp" mà không hỏi yêu cầu thật; kết quả là `CLAUDE.md` đọc một mình thì vô nghĩa | SV | *(chưa commit)* — thay bằng D-006 |
| 3 | 10/09/2026 | AI chốt đặc tả TLCN là "baseline đóng băng", chỉ cho ghi delta (D-004) | AI giả định báo cáo đã chấm điểm thì bất biến, bỏ qua khả năng chính đặc tả đó có chỗ sai/lỗi thời | SV | *(chưa commit)* — thay bằng D-007 |
| 4 | 10/09/2026 | Script kiểm tra link Markdown báo 4 link "GÃY" trong khi cả 4 đều tồn tại | Regex cắt chuỗi không loại bỏ dấu `)` cuối, làm tên file thành `PLAN.md)` — lỗi false positive, suýt dẫn tới "sửa" thứ không hỏng | AI (tự phát hiện khi đối chiếu với `ls`) | *(chưa commit)* |
| 5 | 10/09/2026 | AI đặt tên tài liệu đặc tả là `PRD.md` | AI dùng thuật ngữ phổ biến trong ngành phần mềm thương mại, không đối chiếu Mục 8 của rubric — nơi gọi đích danh hồ sơ là "SRS/SDD" | SV | *(chưa commit)* — xem D-009 |
| 6 | 10/09/2026 | AI đặt tên P-01 là "Khung SDD" trong khi chính AI dùng "SDD" cho tài liệu thiết kế `SDD.md` (D-010) | AI dùng một từ viết tắt cho hai nghĩa (Spec Driven Development và Software Design Document) mà không kiểm tra xung đột trong cùng bộ tài liệu | SV | *(chưa commit)* — đổi thành "Khung specs" |
| 7 | 10/09/2026 | AI chốt hoãn tách SRS/SDD tới sau P-24 (D-010) | AI chỉ cân nhắc công sức tách file, bỏ qua việc tách muộn làm lịch sử Git của phần thiết kế bị cắt ở ngày tách — mất dấu vết quá trình rà soát, vốn là minh chứng rubric chấm | SV | *(chưa commit)* — thay bằng D-011 |
| 8 | 10/09/2026 | AI tách "viết acceptance criteria" (P-20) và "rà soát đặc tả TLCN" (P-23) thành hai việc riêng | AI chia việc theo loại sản phẩm đầu ra thay vì theo cách làm thực tế: cả hai đều phải đi qua cùng 26 use case, tách ra là rà hai lượt | SV | *(chưa commit)* — gộp vào P-23 |
| 9 | 10/09/2026 | AI tạo 2 commit mất dòng tiêu đề `docs: ...` trong commit message | AI nối heredoc với `&& \` trong bash, khiến dòng đầu của heredoc bị hiểu thành một lệnh riêng (`docs:: command not found`) thay vì thành nội dung message | AI (tự phát hiện qua output lỗi, làm lại trước khi push) | `1ad9305`, `43c1a0a` (bản đúng) |
| 10 | 10/09/2026 | Bản nháp đầu của `COMMITMENT.md` ghi CN-08 "không cho vượt tồn kho" và CN-19 "giỏ trống thì AI nhắc" là `Có` | AI chép hành vi từ đặc tả TLCN sang cột hiện trạng, chỉ kiểm tra route tồn tại chứ chưa đọc view. Đọc code thì thấy server không kiểm tra tồn kho (L-7) và phát hiện luôn lỗi giá do trình duyệt gửi lên (L-6) | AI (tự phát hiện khi kiểm chứng trước lúc ghi file) | *(chưa commit)* |
| 11 | 11/09/2026 | `AGENTS.md`/`CLAUDE.md` §3 ghi "Viết test kèm theo (xem §7)" trong khi mục Kiểm thử là §8 (§7 là Quy ước code) | AI viết tham chiếu chéo bằng số mục mà không đối chiếu với tiêu đề thật — sai ngay từ bản commit đầu tiên (`43c1a0a`). Tồn tại qua 3 PR vì checklist review chỉ kiểm tra hai file song sinh khớp *nhau*, không kiểm tra tham chiếu nội bộ có trỏ *đúng* hay không | AI (tự phát hiện khi sửa §3 cho P-07) | `0af7a42` |
| 12 | 12/09/2026 | AI tự ý bỏ hết dấu tiếng Việt trong khối tóm tắt dán lên portal | AI suy đoán portal sẽ lỗi phông khi dán tiếng Việt có dấu, trong khi portal là trang tiếng Việt của khoa — tự đặt ra một ràng buộc không có căn cứ rồi làm hỏng chất lượng văn bản theo nó | SV | `c8159d6` |
| 13 | 12/09/2026 | AI kết luận lỗi "No diagram type detected" của markdown preview là do chuỗi `markdown` sau dấu fence, sửa xong vẫn lỗi | AI đọc mã của extension thấy mermaid chỉ chạy trên phần tử `.mermaid` nhưng vẫn chốt nguyên nhân khi chưa kiểm chứng được; đúng ra phải nói rõ là giả thuyết. Cách sửa cuối cùng là bỏ hẳn khối code ở mục đó — né vấn đề, không phải giải quyết | SV (báo "vẫn bị") | `c8159d6` |
| 14 | 14/09/2026 | Khi ô bảng kế hoạch trong bản xuất đề cương không nhận style riêng, AI kết luận bộ lọc Lua sửa ô "tại chỗ" thì pandoc không ghi nhận, rồi viết lại đoạn code gán ngược từng tầng kèm chú thích khẳng định điều đó — xuất lại vẫn lỗi | AI chốt giả thuyết chỉ vì độ rộng cột đổi được còn style ô thì không, chưa kiểm chứng. Xuất cây AST (`pandoc -t native`) mới thấy Div đã nằm trong ô; nguyên nhân thật là pandoc luôn gán style `Compact` cho khối `Plain` trong bảng. Sửa bằng cách đổi nội dung ô thành `Para`, bỏ đoạn gán ngược và chú thích sai | AI (tự phát hiện khi kiểm chứng bằng AST) | — công cụ xuất đang ở thư mục tạm, chưa vào repo |
| 15 | 14/09/2026 | Dòng "Tp. Hồ Chí Minh, ngày … tháng … năm 2026" trong bản xuất bị dạt về lề trái thay vì nằm trên cột "Người viết đề cương" | AI dùng chung hai tab stop căn giữa của khung ký tên cho dòng ngày, trong khi dòng này chỉ có một ký tự tab — tab rơi vào tab stop của cột trái | AI (so ảnh bản xuất với trang TLCN) | — như trên |
| 16 | 14/09/2026 | Style "Outline Part" in đậm cả dòng, làm "5. PHỤ LỤC — bản cam kết sản phẩm và metric, …" đậm toàn bộ | AI suy từ bản TLCN (mọi dòng phần đều đậm cả dòng) mà không đối chiếu Markdown, nơi chỉ tên phần được đánh dấu `**…**` | AI (so ảnh bản xuất với trang TLCN) | — như trên |
| 17 | 15/09/2026 | D-013 (viết 10/09/2026) ghi TC2.7 "bắt buộc phải có ≥ 10 người dùng thật"; con số này lan sang đề cương (§1, §2.2, bảng kế hoạch) và P-25 | AI lấy ngưỡng Mức 5 của TC2.7 làm điều kiện bắt buộc, không phân biệt các mức (rubric Mục 5: ≥ 3 người là Mức 3, ≥ 5 là Mức 4) và không ghi rằng ngưỡng được điều chỉnh khi chốt bản cam kết | AI (đọc lại rubric khi SV đổi số người thành 3–5) | `9e8dd3e` — ghi chú đính chính dưới D-013, thêm D-017 |
| 18 | 16/09/2026 | Script Python sửa một lượt nhiều tài liệu (`SRS.md`, `PLAN.md`, `AGENTS.md`/`CLAUDE.md`) dừng giữa chừng với `ValueError`, sau khi đã ghi xong `SRS.md` và một phần `PLAN.md` | AI giả định dòng cuối file luôn có ký tự xuống dòng khi tìm chỗ chèn (`s.index("\n", ...)`), trong khi dòng 4 của bảng `PLAN.md` §6 là dòng cuối file và không có. Script lại ghi từng file ngay sau khi sửa, nên lỗi để lại trạng thái sửa dở. AI xem `git diff` để biết phần nào đã ghi rồi làm nốt bằng công cụ Edit; script sau giữ nguyên kiểu xuống dòng và báo rõ chuỗi không khớp | AI (qua output lỗi) | `d045ba7` (`AGENTS.md`/`CLAUDE.md`), `6df5e0f` (`SRS.md`, `PLAN.md`) |
| 19 | 16/09/2026 | Dòng "Tuần 2" của nhật ký tiến độ bị chèn vào bảng mốc thời gian `PLAN.md` §2 (giữa "Bắt đầu" và "Hết Giai đoạn 0") thay vì bảng Nhật ký tiến độ §5; lỗi nằm trong commit `e056d7c` và đã được push | Script tìm chỗ chèn bằng chuỗi `| Tuần 1 |` và lấy vị trí khớp **đầu tiên** mà không kiểm tra chuỗi có duy nhất hay không — chuỗi này khớp cả dòng `| Bắt đầu | Tuần 1 |` ở §2. Sau khi chèn, AI chỉ xem `git diff --stat` (số dòng đổi) chứ không đọc lại vị trí dòng mới, nên lỗi lọt qua 6 commit | AI (đọc lại `PLAN.md` sau khi push) | commit ngay sau `e3473d6`: *docs: move week 2 log row to PLAN section 5* |
| 20 | 17/09/2026 | Khi dựng CI, AI báo với SV rằng `.coveragerc` "chưa bao giờ được commit vì `.gitignore` chặn nó", nên CI sẽ đo độ phủ sai phạm vi — và định sửa `.gitignore`. Thực tế file đã được commit từ P-12 | AI chạy `cat .gitignore | head -60; ls -a grocerly | grep -i cov` trong **một** lệnh; dòng `.coveragerc` in ra từ lệnh `ls` nối ngay sau nội dung `.gitignore`, AI đọc thành một dòng của `.gitignore` rồi kết luận khi chưa kiểm chứng. Chạy `git check-ignore -v` (không trả gì) và `git ls-files` (có file) mới thấy sai. Bài học: gộp nhiều lệnh thì output phải có dấu phân cách; kết luận về trạng thái Git thì kiểm bằng lệnh Git | AI (tự phát hiện trước khi sửa) | — không có gì phải sửa; đã báo lại SV trong phiên |
| 21 | 21/09/2026 | AI khuyên không nên gỡ báo cáo tuần khỏi repo, viện lý do "phụ lục quyển báo cáo cần sổ theo dõi tiến độ 15 tuần" như thể đó là yêu cầu của rubric. Rubric không có yêu cầu nào về phụ lục của quyển báo cáo | AI lấy mục PHỤ LỤC trong `DE_CUONG.md` - văn bản do chính đồ án tự soạn - rồi trình bày nó như ràng buộc từ rubric, không đối chiếu lại bản rubric vừa tải về trong cùng phiên. Lẫn giữa nguồn quy định và nguồn tự khai | SV (hỏi lại "rubric có nhắc tới không") | D-021 |

**Ghi chú khi bảo vệ:** mười trong hai mươi mốt mục trên do sinh viên phát hiện, không phải
AI tự sửa. Đó là điểm cần nói thẳng — nó cho thấy vai trò kiểm soát nằm ở người,
đúng tinh thần TC2.3.

---

## 3. Quy trình kiểm soát đầu ra AI

Mức 5 yêu cầu việc kiểm soát phải **thành hệ thống**, không phải "chạy thử thấy
được thì thôi". Checklist áp dụng cho mọi output của AI trước khi commit:

- [ ] **Đọc hết** phần AI sinh ra. Không commit thứ chưa đọc.
- [ ] **Đối chiếu tài liệu chính thức** (Django docs, docs của thư viện, rubric)
      thay vì tin lời AI — đặc biệt với tên API, tham số, và con số ngưỡng.
- [ ] **Chạy test và linter**, không chỉ chạy thử bằng tay.
- [ ] **Kiểm tra prompt không chứa** secret, khóa API, chuỗi kết nối, dữ liệu
      người dùng thật.
- [ ] **Kiểm tra giấy phép** nếu AI đề xuất chép đoạn mã hoặc thêm thư viện ngoài.
- [ ] **Câu hỏi cuối cùng:** *nếu hội đồng chỉ vào đúng dòng này và cho 3 phút,
      mình có giải thích được nó làm gì, vì sao có mặt ở đây, và bỏ đi thì hệ
      thống hỏng chỗ nào không?* Nếu không → chưa được commit.

---

## Liên kết

- Kế hoạch và backlog: [`PLAN.md`](PLAN.md) — P-04
- Quyết định quan trọng: [`DECISIONS.md`](DECISIONS.md)
- Đặc tả yêu cầu: [`SRS.md`](SRS.md)
- Đặc tả thiết kế: [`SDD.md`](SDD.md)
- Báo cáo tuần: trên portal của trường (không lưu trong repo — D-021)
